@model IEnumerable<ExaminationViewModel>
@{
    ViewData["Title"] = "Quizzes taken by me";
}

<div class="container-fluid pl-5 pr-5 text-center">
    <h3 class="mt-4 mb-4">@ViewData["Title"]</h3>

    <div>
        <div class="row">
            <div class="col-md-2"></div>
            
            <div class="col-md-8">
                <table class="table">
                    <thead>
                    <tr>
                        <th>
                            Quiz name
                        </th>
                        <th>
                            Quiz category
                        </th>
                        <th>
                            Your points
                        </th>
                        <th>
                            Quiz max points
                        </th>
                        <th>
                            Percent correct
                        </th>
                        <th>
                            Date created
                        </th>
                        <th>
                            Actions
                        </th>
                    </tr>
                    </thead>
                    <tbody>
                    @foreach (var examinationViewModel in @Model)
                    {
                        <tr>
                            <td>
                                @examinationViewModel.QuizName
                            </td>
                            <td>
                                @examinationViewModel.QuizCategoryName
                            </td>
                            <td>
                                @examinationViewModel.Points
                            </td>
                            <td>
                                @examinationViewModel.QuizMaxPoints
                            </td>
                            <td>
                                @examinationViewModel.PercentCorrect.ToString("f0")%
                            </td>
                            <td>
                                <time datetime="@examinationViewModel.CreatedOn.ToString("O")">
                                    @examinationViewModel.CreatedOn.ToString("g")
                                </time>
                            </td>
                            <td>
                                <a asp-area="" asp-controller="Quiz" asp-action="View" asp-route-id="@examinationViewModel.Id"
                                   class="btn btn-sm btn-outline-primary">
                                    View
                                </a>
                            </td>

                            @* <td> *@
                            @*     <time datetime="@quiz.ModifiedOn.ToString("O")">@quiz.ModifiedOn.ToString("g")</time> *@
                            @* </td> *@
                        </tr>
                    }
                    </tbody>
                </table>
            </div>
            
            <div class="col-md-2"></div>
        </div>
    </div>
</div>